[project]
name = "py-project-template"
version = "0.1.0"
description = "Scaffold a new Python / ML-research repo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
